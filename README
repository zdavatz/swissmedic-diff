SwissmedicDiff - Find out what Products have changed on the swiss healthcare market
============
Version: 0.1.0
Date: 2008-03-27

Short Description:
Compares two Excel Documents provided by Swissmedic and displays the salient differences


Requirements
------------

  * ruby 1.8
  * parseexcel
	
Install
-------

The easiest way to install is via RubyGems. On the command line enter:

  > gem install swissmedic-diff

To manually install, unpack the archive and use the included setup.rb script.
For example:

  > tar -xvzf swissmedic-diff.tbz
  > cd swissmedic-diff
  > sudo ruby setup.rb


Usage
-----

Usage: /usr/bin/swissmedic-diff [-gnr] <file1> <file2> [<output>]

-g --group         sort by news, deletions and updates
-n --name          sort by name
-r --registration  sort by registration


License
-------

	LGPL

	
Author: Hannes Wyss <hwyss@ywesee.com>
